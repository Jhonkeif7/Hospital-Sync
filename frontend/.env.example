# Copia este archivo como .env.local y rellena los valores desde:
# Supabase → Project Settings → API

# URL del proyecto (solo el dominio, SIN /rest/v1)
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave anon / public
VITE_SUPABASE_ANON_KEY=tu_anon_key
